// Phase 2: Full article scraping with @extractus/article-extractor
// For now, returns null — source reading is skipped in Phase 1

export async function scrapeArticle(
  _url: string
): Promise<string | null> {
  return null;
}
